A custom hook is a function that starts with use and calls like useState or useEffect.
hook differnt from component becuase it uses state and stuff instead of just ui
use hook when shared logic
The local storage one
make it so i can reuse which is helpful
